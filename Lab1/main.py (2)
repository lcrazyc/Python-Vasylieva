# Задане рівняння
r = "4*x + 5 = 25"

# Виводимо заголовок таблиці
print("x=\t\tРівняння=\t\tЧи є данне рівняння коренем")
x = 1

# Поки не знайдено корінь
while True:
    # Обчислюємо ліву частину рівняння
    a = 4 * x + 5
    
    # Виводимо дані в таблицю
    print(f"{x}\t\t\t{a}\t\t\t\t{'Так' if a == 25 else 'Ні'}")
    
    # Перевіряємо, чи ліва частина рівняння дорівнює правій частині
    if a == 25:
        # Знайдено корінь, виходимо з циклу
        break
    else:
        # Якщо ліва частина не дорівнює правій, збільшуємо x на 1 і продовжуємо перевірку
        x += 1
