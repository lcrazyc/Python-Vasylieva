n = int(input("Введіть розмір пірамідки (від 1 до 9): "))

if 1 < n < 9:
    for i in range(n, 0, -1):
        for j in range(1, i + 1):
            print(j, end=" ")
        print()
else:
    print("Введено некоректний розмір. Будь ласка, введіть число від 1 до 9.")
