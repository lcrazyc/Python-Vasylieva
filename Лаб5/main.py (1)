
# Запитуємо користувача про розмір масиву
N = int(input("Введіть кількість елементів у масиві: "))

# Ініціалізуємо пустий масив для зберігання елементів
array = []

# Запитуємо користувача ввести елементи масиву
for i in range(N):
    element = float(input(f"{i + 1}-й елемент: "))
    array.append(element)

# Знаходимо максимальний елемент в масиві
max_element = max(array)

# Виводимо результат
print(f"Максимальний елемент у масиві: {max_element}")
